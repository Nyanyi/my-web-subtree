name: Demo schedule
on: 
  schedule:
    - cron: '15 * * * *'
jobs:
  test_schedule:
    runs-on: ubuntu-latest
    
    steps:
      - name: Every 15
        run: echo "15"
      - name: Update subtree
        run: git subtree pull --squash --prefix=my-web-subtreegit@github.com:Nyanyi/my-web.git main -m "Updating the repository"
      - name: Finishing the job
        run: echo "🍏 This job's status is ${{ job.status }}."
